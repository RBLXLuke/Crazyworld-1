Teleport to <Experience Name>
Event Begins On Friday 25th march 
<NPC> Walk into this portal to experience 24kgoldn exclusive virtual concert
<NPC> Cya Soon!
Hearts Removed
Boombox Removed
Game Version: v3.4 
<Placeholder> (not shown ingame)

local TeleportService = game:GetService("TeleportService")
local gameID = 8967359816 --Game ID

	function onTouched(hit)
		local player = game.Players:GetPlayerFromCharacter(hit.Parent)
		if player then
		TeleportService:Teleport(gameID, player)
	end
	end

script.Parent.Touched:connect(onTouched)
